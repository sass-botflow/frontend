/** Better Auth is hosted on the NestJS backend; frontend proxies this path same-origin. */
export const BETTER_AUTH_BASE_PATH = "/api/auth/better";

/** Default Better Auth session cookie (prefix + session_token). */
export const BETTER_AUTH_SESSION_COOKIE = "better-auth.session_token";
